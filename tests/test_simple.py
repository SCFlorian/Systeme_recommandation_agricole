# Première vérification avec un test simple
def test_ci_cd_pipeline():
    """
    Test minimal pour valider que le pipeline CI/CD fonctionne.
    Ne dépend d'aucune donnée, juste une vérification de base."""

    assert 1 + 1 == 2